# ============================================================
# Conteúdo gerado pelo usuário — NÃO versionar no git
# (ficam direto no servidor da Hostinger, nunca sobrescrever)
# ============================================================
uploads/
videos/
backups/

# Dados do site (gerados/editados pelo painel admin)
# Ficam no servidor — o git nunca deve sobrescrever esses arquivos
salas.json
clientes.json
pontos_estrategicos.json
galeria_home.json

# Arquivos sensíveis do servidor (criados automaticamente pelo PHP)
.admin_credentials
.login_attempts.json
.audit_log.json
db_config.php

# Arquivos de OS e IDE
.DS_Store
Thumbs.db
desktop.ini
.vscode/settings.json

# Logs e temporários
*.log
*.err
*.swp
*.bak
*.tmp
*.orig
